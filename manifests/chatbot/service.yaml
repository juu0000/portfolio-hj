apiVersion: v1
kind: Service
metadata:
  name: chatbot
  namespace: ops-system
  labels:
    app: chatbot
spec:
  ports:
  - port: 80
    targetPort: 5000
  selector:
    app: chatbot
  type: ClusterIP